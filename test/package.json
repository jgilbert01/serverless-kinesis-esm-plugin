{
  "name": "test-service",
  "scripts": {
    "test": "sls package -v -r us-east-1 -s test",
    "dp:lcl": "sls deploy -v -r us-east-1 -s stg",
    "rm:lcl": "sls remove -r us-east-1"
  },
  "dependencies": {},
  "devDependencies": {
    "lodash": "^4.17.21",
    "serverless-kinesis-esm-plugin": "^1.0.0",
    "serverless": "^2.0.0"
  }
}
